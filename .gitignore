# Logs
logs/*.log

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# Local machine scripts (machine-specific paths, not for collaboration)
run_download.bat

# Build artifacts (regenerated by Pipeline/build_all.py, never a source of truth)
/tmp/

# Local Colab-workflow data folder -- notebooks download the dataset here via the
# Kaggle API (see ML Development Environment in ROADMAP.md); never a source of truth,
# the real data lives in Dataset/.
ML/*/data/

# OS
.DS_Store
Thumbs.db
